import { type Ref } from 'react';
import type { Camera } from 'three';
import { Vector2, Vector3 } from 'three';
export type DepthPickingApi = {
    readDepth: (ndc: Vector2 | Vector3) => Promise<number>;
};
export type DepthPickingProps = {
    ref?: Ref<DepthPickingApi>;
};
export declare function DepthPicking({ ref }: DepthPickingProps): null;
export declare function useDepthPicking(pass: React.RefObject<DepthPickingApi | null>, camera?: Camera): (x: number, y: number) => Promise<Vector3 | false>;
